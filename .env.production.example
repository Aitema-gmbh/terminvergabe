# ==========================================
# aitema|Termin – Produktions-Konfiguration
# ==========================================
#
# WICHTIG: Diese Datei ist ein Template!
# Kopieren: cp .env.production.example .env.production
# NIEMALS .env.production in Git einchecken!
#
# OZG-konforme Terminvergabe für das Bürgeramt
#
# Sichere Passwörter generieren:
#   openssl rand -hex 32      (für Passwörter/Secrets)
#   openssl rand -base64 32   (für Base64-Secrets)

# ================================
# PostgreSQL (PFLICHT)
# ================================
POSTGRES_USER=termin_user
POSTGRES_PASSWORD=BITTE_AENDERN_openssl_rand_hex_32
POSTGRES_DB=termin_db

# ================================
# Redis (PFLICHT, für Queue-Management)
# ================================
REDIS_PASSWORD=BITTE_AENDERN_openssl_rand_hex_32

# ================================
# Keycloak SSO (PFLICHT)
# ================================
KEYCLOAK_ADMIN=kcadmin
KEYCLOAK_ADMIN_PASSWORD=BITTE_AENDERN_openssl_rand_hex_32
KEYCLOAK_HOST=sso.IHRE-DOMAIN.de
KEYCLOAK_REALM=termin
KEYCLOAK_CLIENT_ID=termin-backend
KEYCLOAK_CLIENT_SECRET=BITTE_AENDERN_openssl_rand_hex_32

# ================================
# Anwendungs-Secrets (PFLICHT)
# ================================
JWT_SECRET=BITTE_AENDERN_openssl_rand_hex_32
SESSION_SECRET=BITTE_AENDERN_openssl_rand_base64_32
CSRF_SECRET=BITTE_AENDERN_openssl_rand_base64_32

# ================================
# Frontend-URLs (PFLICHT)
# ================================
BUERGER_ORIGIN=https://termin.IHRE-DOMAIN.de
MITARBEITER_ORIGIN=https://termin-intern.IHRE-DOMAIN.de
ADMIN_ORIGIN=https://termin-admin.IHRE-DOMAIN.de
CORS_ORIGINS=https://termin.IHRE-DOMAIN.de,https://termin-intern.IHRE-DOMAIN.de,https://termin-admin.IHRE-DOMAIN.de

# ================================
# E-Mail (für Terminbestätigungen, PFLICHT)
# ================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=termin@IHRE-DOMAIN.de
SMTP_PASSWORD=SMTP_PASSWORT_HIER_EINTRAGEN
SMTP_FROM=Bürgeramt Terminvergabe <termin@IHRE-DOMAIN.de>

# ================================
# SMS (optional, für Terminerinnerungen)
# ================================
SMS_PROVIDER=disabled
# SMS_PROVIDER=twilio
# SMS_TWILIO_ACCOUNT_SID=ACxxxxxxxxxx
# SMS_TWILIO_AUTH_TOKEN=BITTE_AENDERN
# SMS_FROM=+49123456789

# ================================
# Terminbuchungs-Konfiguration (optional)
# ================================
# Maximale Tage im Voraus buchbar
MAX_DAYS_AHEAD=60
# Standard-Terminlänge in Minuten
SLOT_DURATION_MINUTES=15
# Maximale Buchungen pro Bürger pro Tag
MAX_BOOKINGS_PER_DAY=3
# Erinnerungs-E-Mail (Stunden vor Termin)
REMINDER_HOURS_BEFORE=24

# ================================
# Kiosk-Display (optional)
# ================================
# PIN für Kiosk-Display-Zugriff
KIOSK_PIN=1234
# Aktualisierungsintervall in Sekunden
KIOSK_REFRESH_INTERVAL_SECONDS=5

# ================================
# Backup (optional)
# ================================
BACKUP_RETENTION_DAYS=30

# ================================
# Monitoring (optional)
# ================================
# Sentry DSN für Error-Tracking
# SENTRY_DSN=https://...@sentry.io/...

# ================================
# Logging
# ================================
LOG_LEVEL=info
