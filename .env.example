# aitema|Termin – Umgebungsvariablen
# Kopiere diese Datei zu .env und passe die Werte an:
#   cp .env.example .env

# ================================
# Datenbank (PFLICHT)
# ================================
DB_PASSWORD=bitte_aendern_sicheres_passwort
DATABASE_URL=postgresql://aitema:${DB_PASSWORD}@db:5432/termin

# ================================
# Anwendung (PFLICHT)
# ================================
# Öffentliche URL des Bürgerportals
PUBLIC_BASE_URL=http://localhost:5173
# URL des Admin-Backends
ADMIN_BASE_URL=http://localhost:5174

NODE_ENV=production

# ================================
# E-Mail-Bestätigungen (empfohlen)
# ================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=termin@ihre-gemeinde.de

# ================================
# Kiosk-Display (für Warteraumanzeige)
# ================================
# Aktualisierungsintervall in Sekunden
KIOSK_REFRESH_INTERVAL_SECONDS=5

# ================================
# Terminbuchung
# ================================
# Wie weit im Voraus können Termine gebucht werden (in Tagen)?
MAX_DAYS_AHEAD=60

# Standard-Terminlänge in Minuten
SLOT_DURATION_MINUTES=15

# Maximale Termine pro Bürger pro Tag
MAX_BOOKINGS_PER_DAY=3

# ================================
# SMS (optional, für Erinnerungen)
# ================================
# SMS_PROVIDER=
# SMS_API_KEY=
# SMS_FROM=Gemeinde
